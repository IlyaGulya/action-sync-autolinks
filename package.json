{
  "name": "action-sync-autolinks",
  "version": "1.0.0",
  "description": "GitHub Action to sync JIRA queues with repository autolinks",
  "main": "index.js",
  "scripts": {
    "test": "bun test",
    "test:local": "bun test-local.js",
    "build": "bun build index.js --outdir dist --target node --minify"
  },
  "keywords": ["github-action", "jira", "autolinks", "sync"],
  "author": "IlyaGulya",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.19"
  }
}